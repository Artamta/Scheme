(define (eval-expression ))

(define (differiantable x ) )

(define (pow x) )
(define (log x) )
(define (exp x) 
(define (differiantable x ) )

(define (trigo x ) )


(define (deriv x)
  x)
(define ())

(define (x (* x x)))
(display (deriv x))
(newline